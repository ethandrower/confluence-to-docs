# Pinned exactly, deliberately.
#
# These were ranges (`redis>=5.0` and friends) until 2026-08-07, when a deploy's
# rebuild silently moved production from redis-py 7.4.0 to 8.0.1. redis-py 8
# introduced a default 5s socket timeout that tied with channels_redis's 5s
# blocking read and started dropping customers' realtime WebSocket connections
# — no code change, no warning, and unreproducible locally because this machine
# was still on 7.4.0. See portal/tests/test_channel_layer_config.py.
#
# Versions below are what production was running and verified healthy on
# 2026-08-07. To upgrade: change one line, run the suite, deploy, and watch it —
# rather than discovering the change after the fact from a production log.

Django==4.2.30                  # 5.x is a breaking upgrade; also see anymail note
djangorestframework==3.17.1
# OpenAPI schema + Swagger UI for the read-only /api/v1/ integration API.
# Pinned like everything else here: it generates the contract RevenueHub is
# built against, so a silent minor bump could change the published schema
# without anyone touching this repo.
drf-spectacular==0.30.0
inflection==0.5.1               # drf-spectacular dep, pinned for the same reason
uritemplate==4.2.0              # drf-spectacular dep
django-environ==0.14.0
psycopg2-binary==2.9.12
celery==5.6.3
django-celery-beat==2.9.0

# redis + channels: keep these two in step. channels_redis blocks for
# RedisChannelLayer.brpop_timeout (5s) per receive; redis-py's socket_timeout
# must stay clear of it (settings.py sets 20s, and a test asserts the relation).
redis==8.0.1
channels[daphne]==4.3.2
daphne==4.2.2
channels_redis==4.3.0
uvicorn[standard]==0.51.0

lxml==6.1.1
bleach==6.4.0
django-storages==1.14.6
boto3==1.43.45
requests==2.34.2
Pillow==12.3.0

# Production / deploy
gunicorn==26.0.0
whitenoise==6.12.0
# anymail 15 declares django>=5.0, which conflicts with the Django 4.2 pin above
# and breaks a clean `pip install` on the Dokku buildpack. 14.x has the same
# Mailgun backend + tracking-signal API this project uses.
django-anymail[mailgun]==14.0
markdown==3.10.2

# Atlassian Trinity — Confluence/Jira/Bitbucket CLI used by sync_from_mcp.
# Pinned to a commit, not @main: a branch ref means every rebuild can pull
# different code, which is the same class of surprise as an unpinned version.
trinity-atlassian-cli @ git+https://github.com/ethandrower/atlassian-trinity.git@75a513faeed0
